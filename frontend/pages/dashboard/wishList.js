import Layout from '../../components/container/layout'
import WishListContainer from '../../components/container/WishList'

export default function WishList(props) {
    return (
        <Layout>
            <WishListContainer />
        </Layout>
    )
}